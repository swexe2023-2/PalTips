<nav class="navbar navbar-expand-lg bg-dark">
  <%= link_to 'PalTips', root_path, class:"navbar-brand text-light" %>
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <%= form_with url: questions_search_path, method: :get, class: "d-flex", role: "search" do |f| %>
        <%= f.text_field :search, class: "form-control me-2 custom-search", placeholder: "Search", aria: { label: "Search" } %>
        <%= f.submit "Search", class: "btn btn-outline-success text-light" %>
      <% end %>
    </div>
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <% if current_user %>
        <li class="nav-item">
          <%= link_to 'ログアウト', sessions_logout_path, class: 'nav-link text-light' %>
        </li>
        <li class="nav-item">
          <%= link_to 'マイページ', user_path(current_user), class: 'nav-link text-light' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'ログイン', sessions_index_path, class: 'nav-link text-light' %>
        </li>
        
        <li class="nav-item">
          <%= link_to '登録', new_user_path, class: 'nav-link text-light' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>

<style>
.d-flex {
  width:100%;
}
</style>